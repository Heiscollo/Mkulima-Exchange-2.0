import express from 'express';

const router = express.Router();

// POST /api/reviews - Post a review
// GET /api/reviews/:userId - Get user reviews
// GET /api/users/:id/profile - Get user profile with reviews

export default router;
